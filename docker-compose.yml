services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: daily-news-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./.runtime:/app/.runtime
    restart: unless-stopped
